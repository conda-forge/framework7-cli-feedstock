{% set version = "9.0.0" %}

package:
  name: framework7-cli
  version: {{ version }}

source:
  url: https://registry.npmjs.org/framework7-cli/-/framework7-cli-{{ version }}.tgz
  sha256: 28743bd2f9e949f8f00287f7d796ed99575f19dccaa7d24fd76dfd698c34297f

build:
  number: 0
  skip: true  # [win or ppc64le]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - make
    - nodejs
    - pkg-config
    - pnpm
    - pnpm-licenses
    - python >={{ python_min }}
  host:
    - glib
    - libexpat
    - libvips
    - nodejs
  run:
    - nodejs

test:
  commands:
    - framework7 --help

about:
  license: MIT
  license_file:
    - LICENSE
    - third-party-licenses.txt
  summary: Framework7 command line utility (CLI)
  home: https://github.com/framework7io/framework7-cli
  dev_url: https://github.com/framework7io/framework7-cli

extra:
  recipe-maintainers:
    - danielnachun
